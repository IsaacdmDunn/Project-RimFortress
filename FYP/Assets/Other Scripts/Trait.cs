using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Trait : MonoBehaviour
{
    public enum traitType { abrasive = 0, vengeful, kind, sensitive, forgiving};
}
